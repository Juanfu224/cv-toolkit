Tras `cvtool scaffold` aparecen `cv_default_<familia>.yaml` por cada id de `base/familias.yaml`. No edites `cv.html.j2` ni `cv.css` salvo para el motor ATS.

`respuestas.md.j2` es la forma de las respuestas de formulario (pregunta, texto, recuento, fuente, `NECESITA_CONFIRMACION`). El skill **genera candidatura** la sigue al escribir `respuestas.md`.
